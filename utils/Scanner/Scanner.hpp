// the scanner is designed to take the users input and print it out into the array.

//the function will take a string and an array of the tokens 

#pragma once
#include <string>
#include <vector>

std::vector<char> scan_tokens(std::string userInput);



